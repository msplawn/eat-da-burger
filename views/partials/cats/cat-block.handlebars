<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}DEVOUR!{{else}}WAKE UP!{{/if}}
	</button>
	<button class="delete" data-id="{{id}}" >
		FUGGETABOUTIT!
	</button>
</li>
